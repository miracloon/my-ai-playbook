# my-ai-playbook

本仓库是个人 AI 运行产物的已发布分发事实源。`main` 始终保持下游能够按现有 GitHub 路径直接安装的纯净结构；设计讨论、中间稿、评估材料和工作区私有背景不进入这里。

## 分支语义

- `main` 表达当前已经发布、应供各客户端取得的一致版本。
- 短期分支表达尚未发布的具体修改提案，不是第二事实源；完成验证后合并回 `main` 并删除，不建立长期设备分支或工作区分支。
- 确定、局部且已经在真实使用中验证的小修，可以在确认本地主线跟上远端后直接修改 `main`；范围较大、尚有取舍或可能与并行修改交叉的变化使用短期分支。
- 分支与主线并行演化时，由 Git 的共同历史暴露和合并差异，不通过目录时间、双向复制或其他工作区旧副本判断新旧。

## Skill 维护

- `skills/{name}/` 是已发布 Skill 的运行正文。现有 Skill 的后续维护直接发生在本仓库，不从 MAGA 或其他创作工作区的旧 `output/` 整体覆盖。
- MAGA 等工作区可以保存设计依据和首次发布前的编译候选；大型重构需要这些上下文时，在设计工作区维护理由，在本仓库短期分支维护正式候选。
- Skill 首次发布为 `metadata.version: v1.0`。后续版本在修改准备合并或发布时递增；同一个已发布版本不对应多份不同正文。
- `metadata.ry.canonical_source` 是按需使用的个人扩展，不要求所有 Skill 补齐或保持同构。
- 客户端安装副本产生的修补是反馈证据；把有效修改应用到本仓库当前版本，不把安装目录反向镜像成新的权威来源。

修改保持目标目录内聚，只提交当前任务涉及的 Skill、workflow 或 asset。版本恢复交给 Git，不在仓库中保存备份副本、同步状态或兼容旧发布心智的中间文件。
