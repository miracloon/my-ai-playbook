# my-skill-repo

## 用户的 Skill 生态

用户会在多个 Agent 和客户端中复用 Skill，而所有客户端实际使用的都是安装副本。如果每个副本独立维护，同一个问题会被重复修正，内容也会逐渐分叉。

本仓库因此承担个人分发中心和中心事实源：

- 用一份当前正文为不同客户端提供共同基线；
- 让适用于所有环境的修改能够从根源更新，再由各客户端按需取得；
- 让只适用于某个环境的修改留在本地，不污染公共正文。

在分发层面，本仓库始终保存唯一的中心正文；副本取得后是否继续与中心对齐，由 Skill 的 Metadata 决定。中心中的 Skill 不一定被任何客户端使用，客户端也可以拥有不属于中心的普通 Skill。

### 中心与客户端

- **中心事实**：本仓库中的正文回答“当前可供所有客户端取得的 Skill 是什么”。
- **客户端副本**：客户端中的目录回答“这个环境实际安装和使用了什么”。
- **分发**：客户端从中心取得副本；是否安装、更新或启用由客户端决定。
- **反馈**：客户端在真实使用中产生问题、经验和候选修改，为后续维护提供证据。
- **回流**：把具有共享价值的有效修改应用到中心正文，使其他客户端也能取得；回流的是修改，不是用客户端目录反向覆盖中心。

```text
                    ┌─> 客户端 A 副本
中心正文 ──分发─────┼─> 客户端 B 副本
                    └─> 客户端 C 副本

共享修改 ──回流中心──> 各客户端随后按需更新
本地特性 ──留在本地──> 不改变中心正文
```

安装只复制运行内容，不自动产生新的公共事实源。是否允许回流由 Skill 的 Metadata 与用户对当次行动的授权共同决定。

### 本仓库的定位与职责

本仓库是这套生态中的个人 Skill 发布中心，负责：

- 保存每个中心维护 Skill 的唯一当前正文；
- 通过目录组织可分发的 Skill 和共享资源；
- 通过 Metadata 把维护意图传递给不同客户端；
- 接收具有共享价值的设计成果与实战修改。

本仓库不管理客户端的安装、启用和运行状态，也不把设计过程、客户端私有背景或安装副本作为发布正文。

## 目录治理

Skill 使用 `skills-{collection}/{skill-name}/` 的双层结构。第一层按主要领域聚合，第二层是完整、可独立分发的 Skill 目录。

| 目录 | 内容边界 |
|------|----------|
| `skills-common/` | 通用、跨领域，或尚无必要形成独立分类的 Skill |
| `skills-learning/` | 与学习过程、材料理解和认知衔接高度相关的 Skill |
| `skills-research/` | 与科研活动和研究工作高度相关的 Skill |
| `skills-ops/` | 与系统运维、安装、部署和运行维护高度相关的 Skill |
| `skills-hermes/` | 绑定 Hermes 生态、平台事实或运行方式的 Skill |
| `skills-creative/` | 与创意表达和内容创作高度相关的 Skill |
| `assets/` | 多个 Skill 共同使用或分发需要的仓库级资源 |

分类用于形成自然的发现和维护边界，不追求预先完备。新增 Skill 没有明确独立领域时进入 `skills-common/`；现有内容形成清楚、稳定的新聚合关系时再扩展目录。

每个 Skill 只有一个主要物理归属。跨领域可用不产生正文副本；单个 Skill 独有的 references、scripts 和 assets 留在自身目录中。移动 Skill 时，同步检查并更新其 `ry_source_path`。

## Metadata

`metadata` 随 Skill 一起分发，为下游规则提供版本、维护关系和来源血缘。`ry_*` 是用户个人生态使用的可选字段；缺少 `ry_agent_update` 时，Skill 不声明与本中心的维护关系，按普通 Skill 和客户端自身规则处理。

这些字段是语义标记，不是自行执行的策略代码：

- 字段表达稳定的维护意图和修改方向，不规定一套脱离场景的固定操作步骤；
- 下游 Agent 结合字段、客户端规则、当前任务和用户授权决定具体行动；
- 具体做法可以因环境调整，但要保持核心关系：共享修改回到中心，本地特性留在本地，明确禁止的内容不改动。

`ry_agent_update` 决定客户端副本出现修改需求后的去向：

| 值 | 事实关系 | 修改去向 |
|----|----------|----------|
| `source` | 客户端继续以中心正文为事实 | 发现共享问题时优先修改中心，再由其他客户端按需更新 |
| `local` | 中心正文是分发基线，客户端可以形成自己的本地版本 | 只修改本地，不回流；中心更新也不自动覆盖本地变化 |
| `disabled` | 当前正文已经敲定，不交给 Agent 修改 | 不修改中心或本地，只报告问题或提出建议 |

### 字段规则

- `version`：中心发布版本，始终写成带引号的三段字符串，如 `"1.1.0"`。中心正文更新时同步更新版本；同一版本不对应不同中心正文。
- `ry_source_path`：`source` 模式下中心 Skill 相对于仓库根目录的路径。客户端声明仓库根路径，两者共同定位事实源。
- `ry_fork`：该 Skill fork 或主要参考来源的单一 URL，只记录血缘；不建立同步、覆盖或事实权关系。
- metadata 不授予写权限，也不代替用户对当次修改的允许。

`source` 模式下，`ry_source_path` 缺失、失效、越出仓库或指向不同 Skill 时，停止回流并报告，不按名称猜测目标。

```yaml
metadata:
  version: "1.1.0"
  ry_agent_update: source
  ry_source_path: skills-ops/ry-docker-deploy
  ry_fork: https://github.com/example/repository/tree/main/skills/docker-deploy
```

## Skill 维护

修改依据主要来自两类工作区：

- **专门设计 Skill 的工作区**：持有上位治理哲学、设计方法和具体设计上下文，用于创建 Skill，或重新判断定位、触发边界、行为逻辑和结构。
- **发现实战问题的工作区**：在真实任务和客户端环境中提供错误、遗漏、触发偏差、环境冲突和使用体验。

维护时先读取本仓库当前正文，再结合修改的适用范围判断落点：

- 可复用于其他 Agent 和环境的问题，按 `source` 语义修改中心，使后续客户端可以共同更新；
- 只属于当前客户端、Agent 或环境的特性，按 `local` 语义留在本地；
- 已声明为 `disabled` 的 Skill 不直接修改，只保留问题与建议；
- 实战问题触及定位、边界或整体方法时，把证据带入专门设计工作区重新判断。

更新中心时，把确认后的有效修改应用到当前正文并更新版本，不用设计候选或安装目录整体覆盖。客户端何时重新取得或启用新版本，由客户端自行决定。
