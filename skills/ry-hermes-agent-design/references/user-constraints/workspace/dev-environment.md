## 开发环境约定

本工作区不是主要开发项目。开发环境只用于偶发的轻量脚本、小工具、验证和辅助自动化；目标是需要时够用，不把本工作区治理成正式软件工程项目。

引入体积过大的依赖、长期服务、复杂构建链或工程化负担前，应先提醒用户并说明理由。

### Python

本工作区的 Python 开发默认使用 `uv`。除非用户明确同意，不使用系统级 Python 或系统级 `pip` 管理本工作区依赖；确需使用时，先说明原因并获得确认。

### 脚本与工程化负担

一次性验证、迁移和辅助任务优先写成小脚本；除非任务本身需要，不创建完整应用结构。
