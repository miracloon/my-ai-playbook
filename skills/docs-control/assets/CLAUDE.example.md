<!--
=== 写作指导（本区块不进入最终文档）===

【文档定位】
CLAUDE.md 是 Claude 专属的补丁配置文件。Claude Code 会自动加载它。
它的核心作用是引用 AGENTS.md（让 Claude 进入控制面入口）+ 放置 Claude 特有的行为补丁。

【初始化行为】
- CLAUDE.md 是默认创建的轻量文件，不需要询问用户是否创建
- 直接按模板生成，然后顺带问一句"是否有 Claude 专属的补充？"即可
- 不要把注意力集中在此文件上——它是顺手完成的事，不是重要的对齐议题

【已有 CLAUDE.md 的项目】
- 若项目中存在 CLAUDE.bak.md（用户已手动备份的原 CLAUDE.md），
  主动读取其中的内容，将有价值的偏好吸纳到新生成的 CLAUDE.md 中
- CLAUDE.bak.md 的后续处理由用户自行决定，AI 不需要追问

=== 写作指导结束 ===
-->

## Claude 专属补丁

> 本文件为 Claude 专属配置。[`AGENTS.md`](AGENTS.md) 是必须加载的入口文档，本文件作为补充。

---

## 引用

@AGENTS.md

---

## 补充约束

{Claude 专属的行为补丁。如无，保留"暂无"即可。}

- 暂无