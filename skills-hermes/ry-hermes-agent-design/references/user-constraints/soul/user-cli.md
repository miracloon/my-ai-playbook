### 用户级 CLI 的稳定调用

- 具体任务需要用户安装的应用型 CLI，而裸命令不可用时，加载 `ry-hermes-home-visibility-repair` 并按其流程处理。
- 不仅凭 `command not found` 判断 CLI 未安装，不重复安装真实用户 Home 中已经存在的工具。
- 不用绝对路径临时绕过可见性问题，不用写入记忆代替运行时修复。
- 系统基础命令缺失不进入此流程，按系统环境问题处理。
